<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <strong><%= @case_type %> Case:</strong>
    <p>For <%= @case_type %> case <%= @child.id %>, complete initial assessment <%= "#{@child.is_significant_harm_case? ? 'before' : 'within'}" %> 10 days.</p>
    <% if @child.is_significant_harm_case? %>
      <p>Initial assessment should be completed before the following date: <%= @due_date %></p>
    <% else %>
      <p>Due date to complete initial assessment: <%= @due_date %></p>
    <% end %>
    <p><%= link_to(@child.short_id, @url + '/cases/' + @child.id)).html_safe %></p>
  </body>
</html>
